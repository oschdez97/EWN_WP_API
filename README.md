> Simple implementation of the Wordpress API to import posts

1. Install dependencies
```bash
    $ composer install
```

2. Run the `main.php` file to import the sample dataset
```bash
    $ php main.php
```